# version: "3.9"

services:
  api:
    build: ./backend
    ports:
      - "8000:8000"

  app:
    build: ./frontend
    ports:
      - "8501:8501"
    depends_on:
      - api
    environment:
      - API_URL=http://api:8000
